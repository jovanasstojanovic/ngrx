import { ChangeDetectorRef } from '@angular/core';
import { CallService } from '../services/call.service';
import { StreamService } from '../services/stream.service';
import * as i0 from "@angular/core";
/**
 * Trigger get capture screen permissions and send screen to CallService.
 * You can listen to the change and call replaceTrack of peer connection to send the screen capture to that connection
 */
export declare class ShareScreenDirective {
    private streamService;
    private callService;
    private cdr;
    private desktopStream;
    isDisabled: boolean;
    isEnabled: boolean;
    onClick(): Promise<void>;
    constructor(streamService: StreamService, callService: CallService, cdr: ChangeDetectorRef);
    private startShareScreen;
    private stopShareScreen;
    static ɵfac: i0.ɵɵFactoryDeclaration<ShareScreenDirective, never>;
    static ɵdir: i0.ɵɵDirectiveDeclaration<ShareScreenDirective, "[ngxWebrtcShareScreen]", never, {}, {}, never>;
}
